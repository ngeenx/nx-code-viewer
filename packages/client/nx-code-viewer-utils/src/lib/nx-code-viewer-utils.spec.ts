import { nxCodeViewerUtils } from './nx-code-viewer-utils';

describe('nxCodeViewerUtils', () => {
  it('should work', () => {
    expect(nxCodeViewerUtils()).toEqual('nx-code-viewer-utils');
  });
});
